<% blogEntries.forEach((blog) => { %>
    <div>
        <h1><%= blog.title %></h1>
        <img src="<%= blog.image %>">
        <div><%= blog.created %></div>
        <p><%= blog.body %></p>
    </div>
<% }); %>
